kind: pipeline
name: build-deploy

environment:
  BACKEND_NAME: dentist-backend
  FRONT_NAME: dentist-front
  MYSQL_NAME: dentist-mysql
  REPO: registry.cn-hangzhou.aliyuncs.com
  NAMESPACE: shenjies88

steps:
#  - name: restore-cache
  #    image: drillster/drone-volume-cache
  #    volumes:
  #      - name: cache
  #        path: /cache
  #    settings:
  #      restore: true
  #      mount:
  #        - ./.m2
  #        - front-practice/node_modules

  #  - name: build-backend
  #    image: maven:3.6.3-jdk-8
  #    commands:
  #      - mvn -DskipTests clean package -Dmaven.repo.local=./.m2/repository
  #    when:
  #      branch:
  #        - master
  #      event:
  #        - push
  #    depends_on:
  #      - restore-cache

  #  - name: build-front
  #    image: node:latest
  #    commands:
  #      - cd front-practice
  #      - yarn
  #      - yarn build
  #    when:
  #      branch:
  #        - master
  #      event:
  #        - push
  #    depends_on:
  #      - build-backend
  #  - name: rebuild-cache
  #    image: drillster/drone-volume-cache
  #    volumes:
  #      - name: cache
  #        path: /cache
  #    settings:
  #      rebuild: true
  #      mount:
  #        - ./.m2
  #        - front-practice/node_modules
  #    depends_on:
  #      - build-backend

  #  - name: docker-buid-push
  #    image: plugins/docker
  #    settings:
  #      DOCKER_PASSWORD:
  #        from_secret: DOCKER_PASSWORD
  #      DOCKER_USERNAME:
  #        from_secret: DOCKER_USERNAME
  #    commands:
  #      - sh docker_build_push.sh $${DRONE_BUILD_NUMBER} $${BACKEND_NAME} $${FRONT_NAME} $${MYSQL_NAME} $${REPO} $${NAMESPACE}
  #    volumes:
  #      - name: docker
  #        path: /var/run/docker.sock
  #    when:
  #      branch:
  #        - master
  #      event:
  #        - push
  #    depends_on:
  #      - build-backend

  - name: deploy-rancher
    image: registry.cn-hangzhou.aliyuncs.com/lm93129/drone-k8s-update
    settings:
      kubernetes_server: https://192.168.56.20/k8s/clusters/local
      kubernetes_token: token-f2kcd:vhbc2ccchgzbfdb4m9zc7zrbn6mrb284brxstfqnktfbw7j74p5c47
      namespace: default
      deployment: $${MYSQL_NAME}
      repo: $${REPO}/$${NAMESPACE}
      container: $${MYSQL_NAME}
      tag: 109
    when:
      branch:
        - master
      event:
        - push
#    depends_on:
#      - docker-buid-push


volumes:
  - name: cache
    host:
      path: /tmp/cache
  - name: docker
    host:
      path: /var/run/docker.sock


      